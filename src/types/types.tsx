export type Pin = {
  x: number;
  y: number;
  pinId: string;
  color: string;
  countryName: string;
  positioning?: string;
  owner?: string;
  timestamp?: string;
  imgUrl?: string;
  comment?: string;
};
